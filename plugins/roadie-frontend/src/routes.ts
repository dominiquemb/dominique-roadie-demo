import { createRouteRef } from '@backstage/core';

export const rootRouteRef = createRouteRef({
  title: 'roadie-frontend',
});

export const commentsRouteRef = createRouteRef({
  title: 'Comments page'
})